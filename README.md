# ubuntu-toolbox

## 19.10
```
sudo systemctl status systemd-resolved
resolvectl status
dhclient -v -n ens33
nslookup `hostname`.hung.org.hk 127.0.0.53
```
```
ssh-genkey -t rsa
ssh-copy-id hung@192.168.1.139
ssh-agent
ssh-id
```
